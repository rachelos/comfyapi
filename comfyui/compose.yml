services:
 
  comfyui:
    init: true
    container_name: comfyui
    build:
      context: .
      dockerfile: Dockerfile
    image: "yanwk/comfyui-boot:cu126-slim"
    ports:
      - "8188:8188"
    volumes:
      - "./storage:/root" #將目前目录下的storage掛載到容器內部，也可以是其他路徑
    environment:
      - CLI_ARGS=--listen # 加上listen，能让外网存取ComfyUI
    security_opt:
      - "label=type:nvidia_container_t"
    # GPU配置 - 如果有NVIDIA GPU并正确安装了NVIDIA Container Toolkit，请取消注释以下内容
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              # device_ids: ['0']
              capabilities: [gpu]
    